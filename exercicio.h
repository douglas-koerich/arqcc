#ifndef EXERCICIO_H
#define EXERCICIO_H

/* Escreva uma subrotina em Assembly IA-32 ("x86") para ser chamada de uma
 * funcao em C, com o seguinte prototipo:
 */

int rem_multiplos(int* vetor, int* tamanho, int x);

/* A subrotina deve remover de 'vetor' todos os numeros/elementos que sejam
 * multiplos (nao submultiplos) de 'x', retornando o numero de elementos que
 * foram removidos e atualizando o 'tamanho' do vetor que foi passado por
 * referencia/endereco.
 */

#endif

